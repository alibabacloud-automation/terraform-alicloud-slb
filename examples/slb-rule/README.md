# Complete slb-rule example

Configuration in this directory create an SLB Rule instance with Rule.

## Usage

To run this example you need to execute:

```bash
$ terraform init
$ terraform plan
$ terraform apply
```
